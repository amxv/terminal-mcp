[x] revert to simple `tools.json` from v0.40.0, disabled tools can be easily hidden when agent runs `tmcp list`, this makes the entire CLI simple to use and manage. Just one `tools.json` where user can disable tools, which just works because `list` should filter out disabled tools from `tools.json`
[x] Support local stdio servers (including local stdio proxies like `mcp-remote`)
[ ] Support remote SSE servers
[ ] Verify that custom headers, env variables are working for authed servers